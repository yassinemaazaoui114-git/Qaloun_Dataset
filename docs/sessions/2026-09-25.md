# Session Summary — 2026-09-25

- Confirmed the repo is reachable from this environment (clone works) and scaffolded it: `schema.md`, `data/manifest.json`, `scripts/validate.py`, `README.md` — pushed to GitHub manually (this sandbox has no push auth for the repo; connecting GitHub generally didn't fix it — a separate, session-level repo authorization is needed).
- Compared the schema against Tarteel QUL's data model. Redesigned from a single-table sketch into two tables — `quran_layout` (line-level, faithful text) and `quran_words` (word-level, normalized) — needed for a future word-highlighting app, at no extra manual-verification cost.
- Discovered Qaloon uses the Madani First Count (ayah-counting tradition) — distinct from the generic Kufi-count tables most tools use, and from Warsh's Madani Last Count. Validator built to use each surah's own printed `ayah_count` instead of an external table, avoiding the wrong-tradition risk.
- Built and smoke-tested `scripts/validate.py` — correctly caught all planted errors (line-sequence gap, ayah-sequence gap, out-of-order word position) in a test fixture.
- Working process agreed: batch size 10 pages; roughly one fresh conversation per 2–3 batches; two-layer QA (automatic validator, then human check against the source image); model tiers assigned (Sonnet for transcription, Haiku for mechanical fixes, no model needed for the validator, Opus reserved for genuinely ambiguous cases).
- Made the repo self-contained: added `docs/spec.md` + `docs/handoff.md` (mirroring the claude.ai Project's working copy), updated `README.md` to point to them.
- Rewrote `spec.md` clean — removed superseded-design narrative and resolved-question clutter from the main body (kept only in the change log); added permanent §5 (Working Process) and §8 (Repo & Docs Layout) reference sections.
- Agreed a workflow for unfamiliar recitation-notation symbols (e.g. tasheel, sila): check standard Unicode Quranic-annotation range first, then Tarteel QUL, then other Qaloon-specific sources; log resolved ones in `docs/symbols_glossary.md` (not yet created) so nothing gets re-researched.
- Still blocked on: the actual source page images. This sandbox cannot fetch archive.org directly (tested, blocked) — needs upload from the user before batch 1 can start.
